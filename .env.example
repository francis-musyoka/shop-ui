# URL of the Express backend (server-side only). The Next dev proxy rewrites /api/* here.
BACKEND_API_URL=http://localhost:4000

# Absolute URL of this app (client-visible, used in emails and share links).
NEXT_PUBLIC_APP_URL=http://localhost:3000
